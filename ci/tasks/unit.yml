---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

params:
  MAVEN_OPTS:
  MAVEN_CONFIG:

inputs:
  - name: scw-repo

run:
  path: scw-repo/ci/tasks/unit.sh
  args: [
  --input-dir, scw-repo/customer-db
  ]
outputs:
  - name: out
caches:
  - path: /root/.gradle/
  - path: /root/.m2/

